# SRE_DevOps_2026

Linux -- file management, file operations, packetwalk(dns,each layer wise) how the packets flow
bash/python -- classes, OOP
cicd (jenkins, gitlab CI)
kubernetes/helm/docker -- distroless images, identify the best practices and implement them
terraform -- kodecloud.com, best practices, state locking, use modules
Observability -- promotheus and grafana
networking -- 


MCP Tools, create or own mcp tools(customized) -- devops/SRE perspective

python(classes, oops, etc)

Excerise:

Create a small frontend, small backend and a small db. (add, delete, update employees in the table shown on the frontend).
Write CICD for it(build, code quality, package, dockerize, publish the package(artifacts) to jfrog/nexus, set up argocd)
Set up kubernetes, helm charts, deploy through kubernetes/helm charts
frontend, backend and db should be deployed in kubernetes and should be able to communicate with each other and the frontend should be accessible within the network.